language: node_js
node_js:
    - 12.13.1

cache: npm

install: 
    - npm ci

script: 
    - npm run deploy


deploy: 
    provider: pages
    skip-cleanup: true
    github-token: $token
    local_dir: documentation
    on:
        branches: master